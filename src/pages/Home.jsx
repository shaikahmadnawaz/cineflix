import MoviesList from "../components/MoviesList";
import Navbar from "../components/Navbar";

const Home = () => {
  return (
    <section>
      <Navbar />
      <MoviesList />
    </section>
  );
};

export default Home;
